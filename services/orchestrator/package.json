{
  "name": "@netra/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Step Functions task handlers for the Netra investigation lifecycle.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@netra/domain": "workspace:*"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.687.0",
    "@aws-sdk/client-ecs": "^3.1135.0",
    "@aws-sdk/lib-dynamodb": "^3.687.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.9.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
